{
  "name": "using-watch-mode",
  "scripts": {
    "dev": "NODE_OPTIONS=--openssl-legacy-provider webpack --watch",
    "build": "NODE_OPTIONS=--openssl-legacy-provider webpack"
  },
  "devDependencies": {
    "webpack": "^5.30.0",
    "webpack-cli": "^4.6.0"
  }
}
